#include <tunables/global>

profile pushdeer_addon flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability,
  file,
  signal,
  network udp,
  network tcp,
  network dgram,
  network stream,
  network inet,
  network inet6,
  network unix dgram,

  # S6-Overlay
  /init ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /run/{,**} rwk,
  /dev/tty rw,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/lib/bashio/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within your addon
  /data/** rw,
  /config/** rw,
  /share/** rw,
  /ssl/** rw,

  # Apache and PHP
  /etc/apache2/** rw,
  /var/log/apache2/** rw,
  /usr/sbin/httpd ix,
  /usr/bin/php8 ix,

  # Redis
  /usr/bin/redis-server ix,
  /var/run/redis/** rw,
  /etc/redis/** rw,

  # Supervisor
  /usr/bin/supervisord ix,
  /etc/supervisor.d/** rw,
  /var/log/supervisor/** rw,

  # Cron
  /usr/sbin/crond ix,
  /etc/cron.d/** rw,

  # Gorush
  /usr/local/bin/gorush ix,

  # Application files
  /app/** rw,

  # Environment files
  /etc/environment r,
  /etc/php8/** rw,

  # Network access
  network inet tcp,
  network inet udp,
  network inet6 tcp,
  network inet6 udp,

  # Allow signal handling
  signal (send) set=(kill,term,int,hup,cont),
  signal (receive) peer=*,

}